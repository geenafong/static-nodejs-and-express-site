extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 My Portfolio

      p.lead.text-light
        | This portfolio showcases all of the skills that I have acquired throughout my software development journey. Click on one of the projects below to learn more.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each val in data.projects
        .cell
          a(href=`/projects/${val.id}` )
            img.thumbnail(src=`${val.image_urls[0]}`)
            h5 #{val.project_name}
